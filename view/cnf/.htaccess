# Impede acesso HTTP direto a configs e SQL (includes PHP continuam válidos).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Deny from all
</IfModule>
